import React from "react";
import {connect} from "react-redux";

const ContextTypeComponent = (props) => {

    return (
        <>
            ContextTypeComponent
        </>
    )
}

const mapStateToProps = (state: any) => ({
});

const mapDispatchToProps = (dispatch: any) => ({
});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(ContextTypeComponent);

