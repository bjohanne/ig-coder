import React from "react";
import {connect} from "react-redux";

const JunctionChildren = (props) => {

    return (
        <>
            JunctionChildren
        </>
    )
}

const mapStateToProps = (state: any) => ({
});

const mapDispatchToProps = (dispatch: any) => ({
});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(JunctionChildren);

