import React from "react";
import {connect} from "react-redux";

const StatementChildren = (props) => {

    return (
        <>
            StatementChildren
        </>
    )
}

const mapStateToProps = (state: any) => ({
});

const mapDispatchToProps = (dispatch: any) => ({
});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(StatementChildren);

